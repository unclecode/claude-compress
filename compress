#!/bin/bash
# Claude Code Chat Compressor CLI
# Usage: compress [options]
#   compress                     # Default: 30% compression
#   compress --target 50         # 50% compression
#   compress --focus "keep API"  # With focus guidelines
#   compress --help              # Show all options

SCRIPT_DIR="$HOME/.claude/scripts"

# Run the Python script with current directory as --cwd
python3 "$SCRIPT_DIR/compress_chat.py" --cwd "$(pwd)" "$@"
