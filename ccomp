#!/bin/bash
# Claude Code Chat Compressor CLI
# Usage: ccomp [options]
#   ccomp                        # Default: 30% compression
#   ccomp --target 50            # 50% compression
#   ccomp --focus "keep API"     # With focus guidelines
#   ccomp --help                 # Show all options

SCRIPT_DIR="$HOME/.claude/scripts"

# Run the Python script with current directory as --cwd
python3 "$SCRIPT_DIR/ccomp_chat.py" --cwd "$(pwd)" "$@"
